function Triangulo(altura, largura) {
    let area = (altura * largura) / 2;
    return area;
}

const TrianguloAn = function (altura, largura) {
    let area = (altura * largura) / 2;
    return area;
}

const TrianguloAr = (altura, largura) => {
    let area = (altura * largura) / 2;
    return area;
}

