function nmr_de_pi() {
    return 3.14
}

function RaioDaCircunferencia(circunferencia) {
    return circunferencia / (2 * pi());
};
   

const RaioDaCircunferencia =  function (circunferencia) {
    return circunferencia / (2 * pi());
};

const RaioDaCircunferencia = (circunferencia) => {
    return circunferencia / (2 * pi());
}
